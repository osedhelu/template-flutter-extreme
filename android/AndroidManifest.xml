<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.com.extreme.caribemar_wap">
    <!--
      Este archivo se agrega para cumplir con el chequeo del Flutter tooling
      que detecta proyectos antiguos cuando falta `android/AndroidManifest.xml`.

      El manifiesto real de la app se encuentra en:
      `android/app/src/main/AndroidManifest.xml`.
    -->
    <application
        android:label="wap_xcontrol"
        android:name="io.flutter.embedding.android.FlutterApplication"
        android:icon="@mipmap/ic_launcher">

        <!-- Indicador para que Flutter asuma Android embedding v2 en este manifest -->
        <meta-data android:name="flutterEmbedding" android:value="2"/>

        <activity
            android:name="co.com.extreme.caribemar_wap.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
